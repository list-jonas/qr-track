import { LogoutButton } from "@/components/auth/logout";
import React from "react";

const DashboardPage = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <LogoutButton />
    </div>
  );
};

export default DashboardPage;
